### VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/
*.vsix

### JetBrains
.idea/

### Environment — NEVER commit real secrets
# Root .env is committed (contains only dev defaults)
# Local overrides with real secrets must be in .env.local
# Production secret file must never be committed either
.env.local
.env.*.local
.env.prod

### Sub-projects (each has its own git repo)
oracles-api/
thelibrary/
android/

### Composer (project root, if ever used)
vendor/
var/

### OS
.DS_Store
Thumbs.db

.codegraph/
